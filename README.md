
  # CSS for Health System Form

  This is a code bundle for CSS for Health System Form. The original project is available at https://www.figma.com/design/HLmXelfB758r94HAofN7Pu/CSS-for-Health-System-Form.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  